import React from "react";
import classes from "./attachButton.module.css";

export const AttachButton: React.FC = props => {
  return (
    <div className={classes.attachButton}>
      <button />
    </div>
  );
};
